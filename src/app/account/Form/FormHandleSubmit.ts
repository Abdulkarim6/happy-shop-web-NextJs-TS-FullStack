// accessed the input values from the account Form, following the Next.js guide.
{/*
    "use server";
    
    export const FormHandleSubmit = async(formData : FormData) =>{
    
         const rawFormData = {
           name: formData.get("name"),
           email: formData.get("email"),
           password: formData.get("password"),
         };
        console.log("clk", rawFormData);
    }

*/}

{/**
    from client side
    <form action={handleSubmit} className="p-6 md:p-8">
*/}